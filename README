This is a clean reimplementation of the Mozart virtual machine.


Build instructions
==================

mozart$ mkdir build
mozart$ cd build/
build$ cmake ..
build$ make


Run the VM with its hardcoded program
=====================================

build$ cd vm/src/
src$ ./vm
